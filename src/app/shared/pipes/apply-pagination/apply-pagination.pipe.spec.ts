import { ApplyPaginationPipe } from "./apply-pagination.pipe";

describe("ApplyPaginationPipe", () => {
  it("create an instance", () => {
    const pipe = new ApplyPaginationPipe();
    expect(pipe).toBeTruthy();
  });
});
